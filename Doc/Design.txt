2025.6.24
今日无事，不愿做事，便写一编译器，或许可用于STM32

Instruction
	int + name 创建int32变量
	(add/sub/mul/div) + name + (name/num) 四则运算
	set + name + num 
	print + name 输出
	input + name 输入

2025.6.25
今日我又想，既然个人能力有限，何不完成一个虚拟机？如此，编译工作可交由各语言自带的编译器完成。
我只需熟悉字节码即可。我先看了C#的IL，觉其困难。资料也少。又看JVM，似乎简单，但我并不像再安装一门语言，
即使JAVA语法与C#相似。

此项目的一个可能应用是在STM32上用来编写用户应用程序，这样就可以模拟多线程，模拟电脑上的应用执行过程。

(*)对于每个参数，开头为数字，以数字解。否则在已有变量表中查找

//伪指令
int a
//指令
seti a b 0    *a=*b
addi a b c    *a=*b+*c (+-*/%)
printi a 0 0    print(a)
inputi a 0 0  input(a)
stop 0 0 0

//我舍弃了常量池操作

问：为何添加lodi指令？
答：舍弃直接常量池寻址，简化寻址逻辑

第一阶段，我实现了将汇编语言编译为字节码并运行的技术。
现在编译器的输入参数为高级语言代码

优化了输出字节码的质量。

2，加入bool类型
3,bool 运算 
== != 输出为bool。内部是值比较命令

== equ4  != nqu4
>  gtr  <  smr
>= egtr <= esmr

        int/bool
bool b = a==c;

equ4 _A a c
setl b _A

2, > >= < <=					DONE
3，加入if elseif else			DONE

if(EXP){
	EXP;
	EXP;
	EXP;
}


4,函数 name(typ nam,typ nam,)

我在这里卡了很久。还未动笔，脑中混乱的逻辑已经打的不可开交了。今天终于理顺了逻辑：
1，我们的虚拟机是基于寄存器的。现在编译时会引发寄存器溢出异常
2，栈用于函数间通信。	或可用于结构体存储

关于1，因为我们的变量总是随机访问的。自然不是栈。想把它当栈处理自然会引发矛盾。

定义：R0-R19：存任何局部变量 R20：函数返回地址

已经完成了函数划分的代码。现在简化之前的设计，然后应用寄存器的思想。

已经应用了寄存器虚拟机的思想。更改了程序结构后，我们减少了不必要的代码

可以着手攻克函数调用了

完成了参数编译函数。

2025.7.17.10.24 改进版虚拟机试运行

今天改进了条件分支语句的处理算法，使其更简洁。
还实现了while语句

对于for语句，应该在预处理阶段保持其在同一标准行。编译时扩展已有逻辑进行处理 DONE

至此，我们实现了 if elif else while for 5种流程控制语句。

验证其他类型的可行性 done

攻克数组技术

在寄存器中存指针。在堆/栈中存数据

int[] arr = (stackalloc / new) int[9];

我决定取消struct设计。因为在实现上struct就是一个指针
如此：
	class 的访问具有固定的模式
	数组元素有固定的存取方式
	使用 new 或 stackallc 把类分配到栈或堆。可模拟struct行为。

	数组的所有使用形式：

	int[] arr = new int[];
	arr[0] = b;
	b = arr[0];

	St.sb.arr[0].jj.bb[9].a = k;
	_a.a = k;

	arr[0] = b;
	arr[0][6] = b;

	_a.b = ? || _a[9] = ?

	_a. || _a[ => 隔离等号右侧，单独计算后使用setp4指令

struct VarType
{
	int Pdepth （不可引用于publictoken。因为数组类型不必定义在全局范围）
	VarID ID
}
成员变量即偏移。调用publictoken中的函数得到VarType

1，优先级爬山改成从右到左 DONE
3，变vartype为varID，真正的类型使用结构体表示。
4，实现数组

setp4 setp2 setp1: int指针的寻址地址最小偏移距离为4。它不是连续的。

setp4 R1 R2 R3 => R1[R2] = R3
getp4 R1 R2 R3 => R3 = R1[R2]

实现数组							DONE

=========================================

实现类型转换
	(int)			的解析		DONE
	i2b i2s i2f f2i	的实现		DONE
	
	b => s => i		隐式转换		DONE
		精简指令集				DONE

添加char类型						DONE
	setp2 getp2					DONE
	'a'				的解析		DONE  预处理时转成了int
	"string"		的解析		DONE
	
	虚拟机内有“全局区”。存字符串常量和全局变量
	使用lodpz setpz读取并写入全局变量
	lodz R1 <num> | R1 = PZpointer[num];

	先有全局变量，后有字符串常量

	全局变量声明					DONE
	全局变量初始化				DONE
	字符串常量区					DONE

添加自定义类型：class

class ABC
{
	int A;
	int[][]BN;
	char c;
}
ABC nn = new ABC();
ABC[] arr = new ABC[num];

首先，实现立即数寻址				DONE
解析class内存布局				DONE
添加构造函数逻辑					DONE
添加成员运算符					DONE

至此，第一阶段的全部开发目标都已经达成。

单，多行注释功能						DONE

下一步。优化命令行的输入和信息输出		DONE

2025.8.4

对于编译器，我曾尝试过3次。第一次我使用C#。限于技术和概念我未能成功。
第二次我使用C++，但是过于沉迷于优化方面，加上C++不方便DEBUG，导致项目
难以推进。所以第二次我未能制作出解释器。第三次我忽略了编译应该循序渐进
的事实，在开头就加上命名空间和完全的面向对象属性。最后也只实现了分词功能。

这一次我先做出了汇编编译器和解释器。然后完成了整数和局部变量。经历了一些
概念上的梳理，我决定设计基于寄存器的虚拟机。并且更改了四则运算的指令格式，
以匹配布尔运算。后面添加了if else，后面再添加函数，然后是数组，最后是class。
功能由简到繁，减少了重构的压力和设计的难度。
